{
  "name": "pp-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun run migrate && bun src/index.ts",
    "migrate": "bun run src/db/migrate.ts"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "jose": "^5.9.0",
    "nanoid": "^5.0.7",
    "shiki": "^1.22.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.6.0"
  }
}
